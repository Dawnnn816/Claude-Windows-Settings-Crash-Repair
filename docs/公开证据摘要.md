# 公开证据摘要

本文档是 2026 年 8 月 14 日原始诊断证据包的脱敏公开版本。原始证据仍保存在报告者本机，不公开完整注册表导出、完整事件 XML、事件 Report ID、用户目录路径或设置缓存清单。

## 环境

| 项目 | 值 |
| --- | --- |
| Windows 显示版本 | 25H2 |
| OS Build | 26200.8875 |
| 设置包 | `windows.immersivecontrolpanel_10.0.8.1000_neutral_neutral_cw5n1h2txyewy` |
| 设置包状态（修复后） | `Ok` |
| `SystemSettings.exe` 版本 | 10.0.26100.8737 |

系统注册表中的 `ProductName` 显示为 `Windows 10 Home China`，但 Build 26200 属于更新的 Windows 构建系列，因此公开报告以版本和完整 Build 号为准。

## Application Error 汇总

- 事件来源：`Application Error`
- 事件 ID：`1000`
- 异常代码：`0xc0000005`

| 模块 | 事件数 | 固定错误偏移 | 对应页面 |
| --- | ---: | --- | --- |
| `SettingsHandlers_Startup.dll` | 10 | `0x00000000000267b3` | 设置 -> 应用 -> 启动 |
| `SettingsHandlers_StorageSense.dll` | 8 | `0x00000000000d69e3` | 设置 -> 系统 -> 系统组件 -> 高级选项 |

代表性 Startup 事件：

```text
Faulting application name: SystemSettings.exe
Faulting module name: SettingsHandlers_Startup.dll
Exception code: 0xc0000005
Fault offset: 0x00000000000267b3
Faulting application path: C:\Windows\ImmersiveControlPanel\SystemSettings.exe
Faulting module path: C:\Windows\System32\SettingsHandlers_Startup.dll
```

代表性 StorageSense 事件：

```text
Faulting application name: SystemSettings.exe
Faulting module name: SettingsHandlers_StorageSense.dll
Exception code: 0xc0000005
Fault offset: 0x00000000000d69e3
Faulting application path: C:\Windows\ImmersiveControlPanel\SystemSettings.exe
Faulting module path: C:\Windows\System32\SettingsHandlers_StorageSense.dll
```

## StateRepository 相关迹象

`Microsoft-Windows-StateRepository/Operational` 中观察到多条事件 ID `100`：

```text
Error 0x15: misuse at line 185353 of [737ae4a347]
```

这与应用注册/状态层异常相符，但不能单独证明第二条故障的初始来源。

## 文件完整性

以下文件的 Authenticode 状态均为 `Valid`：

- `C:\Windows\ImmersiveControlPanel\SystemSettings.exe`
- `C:\Windows\System32\SettingsHandlers_Startup.dll`
- `C:\Windows\System32\SettingsHandlers_StorageSense.dll`

用户在排查前已执行 `sfc /scannow` 与 `DISM /Online /Cleanup-Image /RestoreHealth`，两条特定崩溃路径仍然存在。

## 启动项差异

修复前：

```text
"\"<用户目录>\AppData\Local\AnthropicClaude\claude.exe\" --startup"
```

修复后：

```text
"<用户目录>\AppData\Local\AnthropicClaude\claude.exe" --startup
```

修复前的值多出一层前置引号。校正后，启动项页面立即恢复。

## 排除测试

| 测试 | 结果 |
| --- | --- |
| 系统文件签名验证 | 相关文件签名有效 |
| Nahimic/A-Volute 服务和计划任务隔离 | 排除为本次触发因素，测试后恢复原状态 |
| 其他 StartupApproved 孤立条目隔离 | 排除为本次触发因素，测试后恢复原状态 |
| 仅重置设置应用包 | 未修复系统组件高级选项崩溃 |
| 重新注册 ImmersiveControlPanel 清单 | 系统组件高级选项恢复 |

## 修复后验证

- “设置 -> 应用 -> 启动”正常打开。
- Microsoft Store 和 Xbox Game Bar 的高级选项正常载入。
- 设置应用包状态为 `Ok`。
- 验证后未产生新的同类崩溃事件。

## 隐私说明

公开仓库刻意不包含以下原始材料：

- 完整 `HKCU\Software\Microsoft\Windows\CurrentVersion\Run` 导出。
- 完整 `StartupApproved` 导出。
- 完整事件 XML 和事件 Report ID。
- 用户名、SID、计算机名及用户目录绝对路径。
- Windows 设置应用本地状态和 WebView 缓存清单。

需要向厂商提交更完整证据时，应通过私密工单渠道单独提供，并在提交前再次脱敏。
